---
- name: Install Dependencies
  apt:
    pkg:
    - nginx-core
    - php
    - php-mbstring
    - imagemagick
    - php-imap
    - jq
    - geoip-database
    - python-openssl
    - python3-psycopg2
    state: latest
    update_cache: yes

    # : nginx, php-fpm (with mbstring, ImageMagick and IMAP), PostgreSQL, GeoIP and jq
